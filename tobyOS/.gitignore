# Build output -- everything here is regenerated by `make`; never commit it.

# Object files / compiled artifacts
*.o
*.pic.o
*.a
*.so

# Linked program + kernel binaries
*.elf
*.bin
tobyos.bin

# Track B (Linux compat): the opt-in musl-static busybox is a third-party
# GPLv2 binary fetched at test time -- never commit it (see
# programs/busybox/README.md for how to obtain it).
programs/busybox/busybox

# Bootable images / filesystems produced by the build
*.iso
disk.img
nvme*.img       # scratch NVMe backing images for QEMU storage tests
sata*.img       # scratch AHCI/SATA backing images for QEMU storage tests
*.tar
build/
iso/

# Disassembly / map dumps
*.dasm
*.map

# Build scratch
.tmpbuild/

# QEMU + capture logs (also covered by the root .gitignore)
qemu*.log
logs/

# Frame-grab scratch from real-HW video debugging
logs/frames*/
