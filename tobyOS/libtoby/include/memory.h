/* memory.h -- traditional alias for <string.h> (mem* functions).
 * Vendored code (h264bsd) includes it; everything lives in string.h. */
#ifndef _MEMORY_H
#define _MEMORY_H
#include <string.h>
#endif
